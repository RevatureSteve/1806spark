package cam.revature;

public class WebDriver {
	
	/*
	 * Comment for a Maven WAR project (web archive)
	 * 
	 * No longer using a main method at all!
	 * Tomcat is the entry point (had the main method)
	 * we provide Servlets to Tomcat to provide logic
	 * on how to handle requests and send back responses
	 * 
	 * THE FIRST 2 things after creating a Maven War project
	 * 		-update java version to 1.8 in pom.xml
	 * 		-generate the web.xml aka deployment descriptor
	 * 			(right-click on project - > java EE tools
	 * -			-> generate deployment descriptor stub
	 * 
	 * 
	 */

}
