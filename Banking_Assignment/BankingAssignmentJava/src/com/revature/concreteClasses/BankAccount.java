package com.revature.concreteClasses;

public class BankAccount {

	protected int accountNumber;//PRIMARY KEY(account_number),
	protected int balance;
	protected int users_id; //FOREIGN KEY (users_id) REFERENCES users(users_id)
	
	
	
	
	
	
	
}
